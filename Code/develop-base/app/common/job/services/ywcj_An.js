/**
 * 任务调度
 */

/**
 * 控制台打印字符串
 */
exports.PrintAn = function() {
    console.log("执行业务酬金环比任务");

};

/*var mysql = require('mysql');
var connection = mysql.createConnection({
    host: '10.201.253.111',
    port : 3306,
    user : 'root',
    password: 'repLcmc0613',
    database: 'ywcj',
    charset : 'UTF8_GENERAL_CI',
    debug : false
});
connection.connect();
connection.query('call zgz_SelectYwc_by_tableAreaTime();', function(err, rows,  fields) {
    if (err) {
        throw err;
    }

   /!* var results = rows[1];
    var row = results[1];
    console.log("result：", row.result);*!/
});

connection.end();*/
